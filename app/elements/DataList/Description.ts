/* tslint:disable:no-magic-numbers */

import { em } from 'polished';

import styled from '../../styled';
import { dataListDescriptionStyle } from './styles';

const DataListDescription = styled.dd`
  ${dataListDescriptionStyle};
`;

export default DataListDescription;
